<!-- この文書はテストの目的と実行方法を説明するためのコメントです。 -->
# テストガイド

## 目的
- 要件定義、詳細要件、コンテンツ定義、設計の文書が存在し、最低限の見出しを含むことを確認する。
- 状態更新と描画文字列の基本ロジックがLuaの単体テストで確認できることを保証する。
- 言語設定の既定値と翻訳の読み取りが行えることを確認する。
- 自動開始の判定ロジックが期待通りに動作することを確認する。

## 実行方法
- ローカル環境で `bash tests/run.sh --env=localdev` を実行する。

## 補足
- テストは文書の読み取りのみを行い、内容の変更は行わない。
